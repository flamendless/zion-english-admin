// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: scheduled_class.sql

package queries

import (
	"context"
	"database/sql"
)

const countScheduledClassesByStatusAndDate = `-- name: CountScheduledClassesByStatusAndDate :one
SELECT COUNT(*) as count
FROM tbl_scheduled_classes
WHERE scheduled_date = ? AND status = ?
  AND (? = 0 OR teacher_id = ?)
`

type CountScheduledClassesByStatusAndDateParams struct {
	ScheduledDate string
	Status        string
	Column3       interface{}
	TeacherID     int64
}

func (q *Queries) CountScheduledClassesByStatusAndDate(ctx context.Context, arg CountScheduledClassesByStatusAndDateParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countScheduledClassesByStatusAndDate,
		arg.ScheduledDate,
		arg.Status,
		arg.Column3,
		arg.TeacherID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countScheduledClassesFiltered = `-- name: CountScheduledClassesFiltered :one
SELECT COUNT(*) as count
FROM tbl_scheduled_classes sc
JOIN tbl_students s ON sc.student_id = s.id
WHERE (? = 0 OR sc.teacher_id = ?) AND sc.scheduled_date >= ? AND sc.scheduled_date <= ?
  AND (? = '' OR sc.status = ?)
  AND (? = '' OR s.name LIKE '%' || ? || '%')
`

type CountScheduledClassesFilteredParams struct {
	Column1         interface{}
	TeacherID       int64
	ScheduledDate   string
	ScheduledDate_2 string
	Column5         interface{}
	Status          string
	Column7         interface{}
	Column8         sql.NullString
}

func (q *Queries) CountScheduledClassesFiltered(ctx context.Context, arg CountScheduledClassesFilteredParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countScheduledClassesFiltered,
		arg.Column1,
		arg.TeacherID,
		arg.ScheduledDate,
		arg.ScheduledDate_2,
		arg.Column5,
		arg.Status,
		arg.Column7,
		arg.Column8,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countScheduledDuplicate = `-- name: CountScheduledDuplicate :one
SELECT COUNT(*) as count
FROM tbl_scheduled_classes
WHERE student_id = ? AND teacher_id = ? AND scheduled_date = ? AND duration_minutes = ?
  AND status = 'scheduled'
  AND (? = 0 OR id != ?)
`

type CountScheduledDuplicateParams struct {
	StudentID       int64
	TeacherID       int64
	ScheduledDate   string
	DurationMinutes int64
	Column5         interface{}
	ID              int64
}

func (q *Queries) CountScheduledDuplicate(ctx context.Context, arg CountScheduledDuplicateParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countScheduledDuplicate,
		arg.StudentID,
		arg.TeacherID,
		arg.ScheduledDate,
		arg.DurationMinutes,
		arg.Column5,
		arg.ID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getScheduledClassByID = `-- name: GetScheduledClassByID :one
SELECT sc.id, sc.student_id, sc.teacher_id, sc.scheduled_date, sc.start_time, sc.duration_minutes, sc.rate, sc.currency, sc.status, sc.reason, sc.created_by_role, sc.created_at, sc.updated_at,
       s.name as student_name,
       trim(t.first_name || CASE WHEN t.middle_name != '' THEN ' ' || t.middle_name ELSE '' END || CASE WHEN t.last_name != '' THEN ' ' || t.last_name ELSE '' END) as teacher_name
FROM tbl_scheduled_classes sc
JOIN tbl_students s ON sc.student_id = s.id
JOIN tbl_teachers t ON sc.teacher_id = t.id
WHERE sc.id = ?
`

type GetScheduledClassByIDRow struct {
	ID              int64
	StudentID       int64
	TeacherID       int64
	ScheduledDate   string
	StartTime       sql.NullString
	DurationMinutes int64
	Rate            float64
	Currency        string
	Status          string
	Reason          sql.NullString
	CreatedByRole   string
	CreatedAt       string
	UpdatedAt       string
	StudentName     string
	TeacherName     string
}

func (q *Queries) GetScheduledClassByID(ctx context.Context, id int64) (GetScheduledClassByIDRow, error) {
	row := q.db.QueryRowContext(ctx, getScheduledClassByID, id)
	var i GetScheduledClassByIDRow
	err := row.Scan(
		&i.ID,
		&i.StudentID,
		&i.TeacherID,
		&i.ScheduledDate,
		&i.StartTime,
		&i.DurationMinutes,
		&i.Rate,
		&i.Currency,
		&i.Status,
		&i.Reason,
		&i.CreatedByRole,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.StudentName,
		&i.TeacherName,
	)
	return i, err
}

const getScheduledClassesByStudentOnDate = `-- name: GetScheduledClassesByStudentOnDate :many
SELECT id, start_time, duration_minutes
FROM tbl_scheduled_classes
WHERE student_id = ? AND scheduled_date = ? AND status = 'scheduled'
  AND start_time IS NOT NULL AND trim(start_time) != ''
  AND (? = 0 OR id != ?)
`

type GetScheduledClassesByStudentOnDateParams struct {
	StudentID     int64
	ScheduledDate string
	Column3       interface{}
	ID            int64
}

type GetScheduledClassesByStudentOnDateRow struct {
	ID              int64
	StartTime       sql.NullString
	DurationMinutes int64
}

func (q *Queries) GetScheduledClassesByStudentOnDate(ctx context.Context, arg GetScheduledClassesByStudentOnDateParams) ([]GetScheduledClassesByStudentOnDateRow, error) {
	rows, err := q.db.QueryContext(ctx, getScheduledClassesByStudentOnDate,
		arg.StudentID,
		arg.ScheduledDate,
		arg.Column3,
		arg.ID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetScheduledClassesByStudentOnDateRow
	for rows.Next() {
		var i GetScheduledClassesByStudentOnDateRow
		if err := rows.Scan(&i.ID, &i.StartTime, &i.DurationMinutes); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getScheduledClassesByTeacherOnDate = `-- name: GetScheduledClassesByTeacherOnDate :many
SELECT id, start_time, duration_minutes
FROM tbl_scheduled_classes
WHERE teacher_id = ? AND scheduled_date = ? AND status = 'scheduled'
  AND start_time IS NOT NULL AND trim(start_time) != ''
  AND (? = 0 OR id != ?)
`

type GetScheduledClassesByTeacherOnDateParams struct {
	TeacherID     int64
	ScheduledDate string
	Column3       interface{}
	ID            int64
}

type GetScheduledClassesByTeacherOnDateRow struct {
	ID              int64
	StartTime       sql.NullString
	DurationMinutes int64
}

func (q *Queries) GetScheduledClassesByTeacherOnDate(ctx context.Context, arg GetScheduledClassesByTeacherOnDateParams) ([]GetScheduledClassesByTeacherOnDateRow, error) {
	rows, err := q.db.QueryContext(ctx, getScheduledClassesByTeacherOnDate,
		arg.TeacherID,
		arg.ScheduledDate,
		arg.Column3,
		arg.ID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetScheduledClassesByTeacherOnDateRow
	for rows.Next() {
		var i GetScheduledClassesByTeacherOnDateRow
		if err := rows.Scan(&i.ID, &i.StartTime, &i.DurationMinutes); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getScheduledClassesFiltered = `-- name: GetScheduledClassesFiltered :many
SELECT sc.id, sc.student_id, sc.teacher_id, sc.scheduled_date, sc.start_time, sc.duration_minutes, sc.rate, sc.currency, sc.status, sc.reason, sc.created_by_role, sc.created_at, sc.updated_at,
       s.name as student_name,
       trim(t.first_name || CASE WHEN t.middle_name != '' THEN ' ' || t.middle_name ELSE '' END || CASE WHEN t.last_name != '' THEN ' ' || t.last_name ELSE '' END) as teacher_name,
       t.first_name as teacher_first_name, t.middle_name as teacher_middle_name, t.last_name as teacher_last_name,
       t.assigned_color as teacher_assigned_color, t.profile_picture as teacher_profile_picture
FROM tbl_scheduled_classes sc
JOIN tbl_students s ON sc.student_id = s.id
JOIN tbl_teachers t ON sc.teacher_id = t.id
WHERE (? = 0 OR sc.teacher_id = ?) AND sc.scheduled_date >= ? AND sc.scheduled_date <= ?
  AND (? = '' OR sc.status = ?)
  AND (? = '' OR s.name LIKE '%' || ? || '%')
ORDER BY CASE WHEN sc.scheduled_date = date('now', 'localtime') THEN 0 ELSE 1 END, sc.scheduled_date ASC, sc.start_time ASC, sc.created_at ASC
LIMIT ? OFFSET ?
`

type GetScheduledClassesFilteredParams struct {
	Column1         interface{}
	TeacherID       int64
	ScheduledDate   string
	ScheduledDate_2 string
	Column5         interface{}
	Status          string
	Column7         interface{}
	Column8         sql.NullString
	Limit           int64
	Offset          int64
}

type GetScheduledClassesFilteredRow struct {
	ID                    int64
	StudentID             int64
	TeacherID             int64
	ScheduledDate         string
	StartTime             sql.NullString
	DurationMinutes       int64
	Rate                  float64
	Currency              string
	Status                string
	Reason                sql.NullString
	CreatedByRole         string
	CreatedAt             string
	UpdatedAt             string
	StudentName           string
	TeacherName           string
	TeacherFirstName      string
	TeacherMiddleName     string
	TeacherLastName       string
	TeacherAssignedColor  string
	TeacherProfilePicture sql.NullString
}

func (q *Queries) GetScheduledClassesFiltered(ctx context.Context, arg GetScheduledClassesFilteredParams) ([]GetScheduledClassesFilteredRow, error) {
	rows, err := q.db.QueryContext(ctx, getScheduledClassesFiltered,
		arg.Column1,
		arg.TeacherID,
		arg.ScheduledDate,
		arg.ScheduledDate_2,
		arg.Column5,
		arg.Status,
		arg.Column7,
		arg.Column8,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetScheduledClassesFilteredRow
	for rows.Next() {
		var i GetScheduledClassesFilteredRow
		if err := rows.Scan(
			&i.ID,
			&i.StudentID,
			&i.TeacherID,
			&i.ScheduledDate,
			&i.StartTime,
			&i.DurationMinutes,
			&i.Rate,
			&i.Currency,
			&i.Status,
			&i.Reason,
			&i.CreatedByRole,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.StudentName,
			&i.TeacherName,
			&i.TeacherFirstName,
			&i.TeacherMiddleName,
			&i.TeacherLastName,
			&i.TeacherAssignedColor,
			&i.TeacherProfilePicture,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertScheduledClass = `-- name: InsertScheduledClass :one
INSERT INTO tbl_scheduled_classes (student_id, teacher_id, scheduled_date, start_time, duration_minutes, rate, currency, status, reason, created_by_role)
VALUES (?, ?, ?, ?, ?, ?, ?, 'scheduled', ?, ?)
RETURNING id
`

type InsertScheduledClassParams struct {
	StudentID       int64
	TeacherID       int64
	ScheduledDate   string
	StartTime       sql.NullString
	DurationMinutes int64
	Rate            float64
	Currency        string
	Reason          sql.NullString
	CreatedByRole   string
}

func (q *Queries) InsertScheduledClass(ctx context.Context, arg InsertScheduledClassParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, insertScheduledClass,
		arg.StudentID,
		arg.TeacherID,
		arg.ScheduledDate,
		arg.StartTime,
		arg.DurationMinutes,
		arg.Rate,
		arg.Currency,
		arg.Reason,
		arg.CreatedByRole,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}

const rescheduleScheduledClass = `-- name: RescheduleScheduledClass :exec
UPDATE tbl_scheduled_classes
SET scheduled_date = ?, start_time = ?, status = 'scheduled', reason = ?, updated_at = datetime('now')
WHERE id = ?
`

type RescheduleScheduledClassParams struct {
	ScheduledDate string
	StartTime     sql.NullString
	Reason        sql.NullString
	ID            int64
}

func (q *Queries) RescheduleScheduledClass(ctx context.Context, arg RescheduleScheduledClassParams) error {
	_, err := q.db.ExecContext(ctx, rescheduleScheduledClass,
		arg.ScheduledDate,
		arg.StartTime,
		arg.Reason,
		arg.ID,
	)
	return err
}

const updateScheduledClassDetails = `-- name: UpdateScheduledClassDetails :exec
UPDATE tbl_scheduled_classes
SET student_id = ?, rate = ?, currency = ?, updated_at = datetime('now')
WHERE id = ?
`

type UpdateScheduledClassDetailsParams struct {
	StudentID int64
	Rate      float64
	Currency  string
	ID        int64
}

func (q *Queries) UpdateScheduledClassDetails(ctx context.Context, arg UpdateScheduledClassDetailsParams) error {
	_, err := q.db.ExecContext(ctx, updateScheduledClassDetails,
		arg.StudentID,
		arg.Rate,
		arg.Currency,
		arg.ID,
	)
	return err
}

const updateScheduledClassStatus = `-- name: UpdateScheduledClassStatus :exec
UPDATE tbl_scheduled_classes
SET status = ?, reason = ?, updated_at = datetime('now')
WHERE id = ?
`

type UpdateScheduledClassStatusParams struct {
	Status string
	Reason sql.NullString
	ID     int64
}

func (q *Queries) UpdateScheduledClassStatus(ctx context.Context, arg UpdateScheduledClassStatusParams) error {
	_, err := q.db.ExecContext(ctx, updateScheduledClassStatus, arg.Status, arg.Reason, arg.ID)
	return err
}
