package frontend

import "zion-english/internal/utils"

templ TeacherDropdown() {
	<div class="form-group">
		<label for="teacher">Teacher *</label>
		<select id="teacher" name="teacher" required
			hx-get={ utils.URL("/api/teachers") }
			hx-trigger="load"
			hx-swap="innerHTML"
			hx-target="this"
		>
			//TeacherOption
			<option value="">-- Select Teacher --</option>
		</select>
	</div>
}

templ TeacherDropdownEx(label string) {
	<div class="form-group">
		<label for="teacher">{ label }</label>
		<select id="teacher" name="teacher" required
			hx-get={ utils.URL("/api/teachers") }
			hx-trigger="load"
			hx-swap="innerHTML"
			hx-target="this"
		>
			//TeacherOption
			<option value="">-- Select Teacher --</option>
		</select>
	</div>
}

templ TeacherStudentsDropdown() {
	<div class="form-group">
		<label for="student">Student *</label>
		<select id="student" name="student" required
			hx-get={ utils.URL("/api/me/students") }
			hx-trigger="load"
			hx-swap="innerHTML"
			hx-target="this"
		>
			//StudentOption
			<option value="">-- Select Student --</option>
		</select>
	</div>
}

templ StudentDropdown() {
	<div class="form-group">
		<label for="student">Student *</label>
		<select id="student" name="student" required
			hx-get={ utils.URL("/api/students") }
			hx-trigger="load"
			hx-swap="innerHTML"
			hx-target="this"
		>
			//StudentOption
			<option value="">-- Select Student --</option>
		</select>
	</div>
}

templ ScheduleModalTeacherDropdown() {
	<div class="form-group">
		<label for="schedule_teacher">Teacher *</label>
		<select id="schedule_teacher" name="schedule_teacher" required
			hx-get={ utils.URL("/api/teachers") }
			hx-trigger="load"
			hx-swap="innerHTML"
			hx-target="this"
		>
			<option value="">-- Select Teacher --</option>
		</select>
	</div>
}

templ ScheduleModalStudentDropdown() {
	<div class="form-group">
		<label for="schedule_student">Student *</label>
		<select id="schedule_student" name="schedule_student" required>
			<option value="">-- Select teacher first --</option>
		</select>
	</div>
}

templ StudentSearchbox() {
	<div class="form-group student-search">
		<label for="relatedStudentSearch">Existing Student</label>
		<input
			type="text"
			id="relatedStudentSearch"
			name="q"
			autocomplete="off"
			placeholder="Search registered student..."
			hx-get={ utils.URL("/api/students/search") }
			hx-trigger="input changed delay:300ms"
			hx-target="#relatedStudentResults"
			hx-include="this"
		/>
		<input type="hidden" id="relatedStudentId" name="relatedStudentId"/>
		<div id="relatedStudentResults"></div>
	</div>
}

templ CurrencyDropdownWithValue(selected string) {
	<div class="form-group">
		<label for="currency">Currency *</label>
		<select id="currency" name="currency" required>
			<option value="KRW" selected?={ selected == "KRW" }>KRW (Korean Won)</option>
			<option value="CAD" selected?={ selected == "CAD" }>CAD (Canadian Dollar)</option>
			<option value="YEN" selected?={ selected == "YEN" }>YEN (Japanese Yen)</option>
			<option value="PHP" selected?={ selected == "PHP" }>PHP (Philippine Peso)</option>
		</select>
	</div>
}

templ ClassStatusDropdownWithValue(selected string) {
	<div class="form-group">
		<label for="status">Status *</label>
		<select id="status" name="status" required>
			<option value="conducted" selected?={ selected == "conducted" }>Conducted</option>
			<option value="cancelled" selected?={ selected == "cancelled" }>Cancelled</option>
			<option value="rescheduled" selected?={ selected == "rescheduled" }>Rescheduled</option>
		</select>
	</div>
}

templ StudentStatusDropdownWithValue(selected string) {
	<div class="form-group">
		<label for="status">Status *</label>
		<select id="status" name="status" required>
			<option value="active" selected?={ selected == "active" }>Active</option>
			<option value="inactive" selected?={ selected == "inactive" }>Inactive</option>
		</select>
	</div>
}

templ CurrencyDropdown() {
	<div class="form-group">
		<label for="currency">Currency *</label>
		<select id="currency" name="currency" required>
			<option value="">-- Select Currency --</option>
			<option value="KRW">KRW (Korean Won)</option>
			<option value="CAD">CAD (Canadian Dollar)</option>
			<option value="YEN">YEN (Japanese Yen)</option>
			<option value="PHP">PHP (Philippine Peso)</option>
		</select>
	</div>
}

templ ClassStatusDropdown() {
	<div class="form-group">
		<label for="status">Status *</label>
		<select id="status" name="status" required>
			<option value="conducted">Conducted</option>
			<option value="cancelled">Cancelled</option>
			<option value="rescheduled">Rescheduled</option>
		</select>
	</div>
}

templ StudentStatusDropdown() {
	<div class="form-group">
		<label for="status">Status *</label>
		<select id="status" name="status" required>
			<option value="active">Active</option>
			<option value="inactive">Inactive</option>
		</select>
	</div>
}

var letters = []string{"A", "B", "C", "D", "E", "F", "G", "H"}
templ Letters(id string, text string, initial string) {
	<div class="form-group">
		<label for={ id }>{ text }</label>
		<select id={ id } name={ id } required>
			for _, letter := range letters {
				<option value={ letter } selected?={ letter == initial }>{  letter }</option>
			}
		</select>
		{ children... }
	</div>
}
